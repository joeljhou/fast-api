version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: fastapi-local:latest
    container_name: fastapi-local
    ports:
      - "2345:8000"
    volumes:
      - ./app:/app/app:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped